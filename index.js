import 'react-native-url-polyfill/auto';

import { registerRootComponent } from 'expo';
import App from './App';

// Expo necesita que el componente raíz quede registrado.
registerRootComponent(App);

